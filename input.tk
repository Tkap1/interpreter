


for p_: 100
{
	int p = p_ + 2;
	int is_prime = 1;
	for check_: p / 2 + 1
	{
		int check = check_ + 2;
		int m = p % check;
		if m == 0
		{
			is_prime = 0;
		}
	}
	if is_prime == 1
	{
		print(p);
	}
}
